<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.udgapk">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EmailActivity"
            android:theme="@style/AppThemeDashboard"
            />
        <activity
            android:name=".MikroActivity"
            android:theme="@style/AppThemeDashboard" />
        <activity
            android:name=".DiscussionActivity"
            android:theme="@style/AppThemeDashboard" />
        <activity
            android:name=".OnamaActivity"
            android:theme="@style/AppThemeDashboard" />
        <activity
            android:name=".CaskanjeActivity"
            android:theme="@style/AppThemeDashboard" />
        <activity
            android:name=".KvizActivity"
            android:theme="@style/AppThemeDashboard" />
        <activity
            android:name=".PredmetiActivity"
            android:theme="@style/AppThemeDashboard" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppThemeDashboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>